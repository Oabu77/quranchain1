#cloud-config
# Legacy Wi-Fi gateway provisioning is quarantined. This inert cloud-init
# performs no package, routing, radio, download, registration, or service work.
runcmd:
  - ["/bin/echo", "DarCloud legacy gateway provisioning is quarantined"]
final_message: "DarCloud inert pre-release image initialized"
