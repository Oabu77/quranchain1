#cloud-config
# Legacy FungiMesh provisioning is quarantined. This inert cloud-init performs
# no installation, registration, networking, tunnel, service, or startup work.
runcmd:
  - ["/bin/echo", "DarCloud legacy mesh provisioning is quarantined"]
final_message: "DarCloud inert pre-release image initialized"
